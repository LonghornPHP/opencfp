#!/bin/sh

sv_stop() {
    for s in /etc/service/*;
    do
        /sbin/sv stop "$s"
    done
}

chown -R nginx:nginx /srv/application/web/uploads /srv/application/cache/production /srv/application/log

trap "sv_stop; exit" SIGTERM
/sbin/runsvdir /etc/service &
wait
